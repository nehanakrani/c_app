<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <%= link_to 'ChatRoomApp', root_path, class: "navbar-brand" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <% if user_signed_in? %>
            <%= link_to 'Sign out', destroy_user_session_path, class: "btn btn-primary btn-right", data: { turbo_method: :delete } %>
        <% else %>
            <h1>Home</h1>
            <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-primary btn-right' %>
        <% end %>
      </div>
    </div>
  </nav>
</header>
